# Imagens pendentes

Esta pasta ainda não contém capturas de tela. Sugestões de imagens a adicionar futuramente (nenhuma foi criada ou inventada):

- `circulo.png` — janela do `Circulo` exibindo o círculo desenhado pelo algoritmo do ponto médio.
- `elipse.png` — janela do `Elipse` exibindo a elipse desenhada e/ou preenchida.
- `poligono.png` — janela do `Poligono` exibindo o polígono desenhado e preenchido (scanline fill).
- `triangulo.png` — janela do `Triangulo` exibindo o triângulo original e uma transformação aplicada (escala, rotação, translação ou espelhamento).

Ao adicionar as imagens, referencie-as no `README.md` principal (seção de descrição ou uma seção "Screenshots").
